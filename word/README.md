# Генератор Anki колоды для английских слов

Эта папка содержит инструменты для создания Anki колоды для изучения английских слов с переводом на русский.

## Структура файлов

- `words.csv` - CSV файл со словами (редактируйте этот файл для добавления своих слов)
- `generate_words_deck.py` - Python скрипт для генерации Anki колоды
- `requirements.txt` - зависимости Python
- `generate.bat` / `generate.sh` - скрипты для быстрого запуска
- `english_words_list.pdf` - исходный PDF файл со словами
- `DEVELOPERS.md` - документация для разработчиков
- `templates/` - папка с шаблонами:
  - `styles.css` - CSS стили для карточек
  - `tts_button.js` - JavaScript для озвучивания
  - `check_answer.js` - JavaScript для проверки ответов
  - `card_*.html` - HTML шаблоны карточек

## Формат CSV файла

CSV файл должен содержать следующие колонки:

```csv
word,transcription,translation,example_en,example_ru,audio_url
```

### Описание полей:

- **word** - английское слово
- **transcription** - транскрипция в квадратных скобках (например, [əˈbændən])
- **translation** - перевод на русский (можно указать несколько вариантов через точку с запятой)
- **example_en** - пример использования на английском
- **example_ru** - перевод примера на русский
- **audio_url** - ссылка на аудио файл с произношением (необязательно)

### Пример строки:

```csv
abandon,[əˈbændən],покидать; оставлять,They had to abandon the car.,Им пришлось бросить машину.,https://example.com/audio/abandon.mp3
```

## Установка

1. Установите Python 3.7 или выше
2. Установите зависимости:

```bash
pip install -r requirements.txt
```

## Использование

### Способ 1: Использование скрипта

**Windows:**
```bash
generate.bat
```

**Linux/Mac:**
```bash
chmod +x generate.sh
./generate.sh
```

### Способ 2: Ручной запуск

```bash
python generate_words_deck.py words.csv -o english_words.apkg -n "English Words"
```

#### Параметры:

- `words.csv` - путь к CSV файлу со словами
- `-o` или `--output` - имя выходного файла (по умолчанию: `english_words.apkg`)
- `-n` или `--name` - название колоды в Anki (по умолчанию: `English Words`)

## Типы карточек

Генератор создает 3 типа карточек для каждого слова:

### 1. EN → RUS (Английское слово → Русский перевод)
- **Вопрос:** Показывает английское слово с транскрипцией
- **Ответ:** Нужно ввести русский перевод

### 2. RUS → EN (Русский перевод → Английское слово)
- **Вопрос:** Показывает русский перевод
- **Ответ:** Нужно ввести английское слово

### 3. Example Practice (Практика с примерами)
- **Вопрос:** Показывает русский перевод и русское предложение-пример
- **Ответ:** Нужно составить английское предложение

## Особенности

- ✅ Встроенный синтезатор речи (кнопка 🔊 TTS) для озвучивания слов
- ✅ Поддержка внешних аудио файлов (кнопка 🔊 Audio)
- ✅ Проверка правильности ответов
- ✅ Транскрипция для каждого слова
- ✅ Примеры использования на обоих языках
- ✅ Поддержка нескольких вариантов перевода

## Добавление слов из PDF

Чтобы добавить слова из файла `english_words_list.pdf`:

1. Откройте PDF файл
2. Скопируйте нужные слова
3. Добавьте их в файл `words.csv` в правильном формате
4. Запустите скрипт генерации заново

## Импорт в Anki

1. Откройте Anki
2. Файл → Импортировать
3. Выберите сгенерированный файл `english_words.apkg`
4. Нажмите "Импорт"

## Поиск аудио файлов

Вы можете использовать аудио файлы из различных источников:

- Google Dictionary API: `https://ssl.gstatic.com/dictionary/static/sounds/20200429/[word]--_us_1.mp3`
- Forvo.com - база произношений от носителей языка
- Cambridge Dictionary
- Oxford Learner's Dictionaries

Просто вставьте URL в колонку `audio_url` в CSV файле.

## Кастомизация

Вы можете легко изменить внешний вид и поведение карточек:

### Изменить стили
Отредактируйте файл `templates/styles.css`

### Изменить логику проверки
Отредактируйте файл `templates/check_answer.js`

### Изменить структуру карточек
Отредактируйте файлы `templates/card_*.html`

Подробнее см. в `templates/README.md` и `DEVELOPERS.md`

## Лицензия

Этот проект создан для личного использования в изучении английского языка.

